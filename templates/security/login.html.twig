{% extends 'base.html.twig' %}

{% block title %}Log in!
{% endblock %}

{% block body %}
	<form method="post">
		{% if error %}
			<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
		{% endif %}

		{% if app.user %}
			<div class="mb-3">
				Vous êtes connecté en tant que
				{{ app.user.userIdentifier }},
				<a href="{{ path('app_logout') }}">Déconnexion</a>
			</div>
		{% endif %}

		<div class="container col-xl-10 col-xxl-8 px-4 py-5">
			<div class="row align-items-center g-lg-5 py-5">
				<div class="col-lg-7 text-center text-lg-start">
					<h1 class="display-4 fw-bold lh-1 mb-3 text-info fs-4">Cet espace est réservé au personnel</h1>
					<p class="col-lg-10 fs-6">Nulla nec velit eget ligula molestie laoreet at at erat. Integer eget tellus eget orci venenatis eleifend vitae vel purus.
					</p>
				</div>
				<div class="col-md-10 mx-auto col-lg-5">
					<form class="p-4 p-md-5 rounded-3 ">
						<div class=" mb-3 row">
							<label for="inputEmail" class="fs-6 text-secondary">Email :</label>
							<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control text-primary border-secondary" autocomplete="email" required autofocus>

						</div>
						<div class="mb-3 row">
							<label for="inputPassword" class="fs-6 text-secondary">Mot de passe :</label>
							<input type="password" name="password" id="inputPassword" class="form-control text-primary border-secondary" autocomplete="current-password" required>

							<input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

						</div>
						<div class="row d-grid justify-content-center">
							<button class="btn rounded-5 btn-secondary text-light" type="submit">Me Connecter</button>

						</div>

					</form>
				</div>
				<small class="text-secondary">Lorsque vous clickez sur connexion vous acceptez
					<a href="{{path('app_legal')}}">nos conditions d'utilisation</a>.</small>
			</div>
		</div>


	{% endblock %}
